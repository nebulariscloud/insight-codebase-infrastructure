# This leaf's terraform.tfvars contains only the account label, public
# 12-digit account ID, AMI/snapshot IDs, private IP, and CIDR allow-lists.
# All safe to commit. Override the parent terraform/.gitignore so CI can read
# it via -var-file=terraform.tfvars.
!terraform.tfvars
