# This leaf's terraform.tfvars contains only the account label, public
# account ID, VPC/subnet IDs, and the Wazuh manager private IP - all safe
# to commit. Override the parent terraform/.gitignore so CI can read it
# via -var-file=terraform.tfvars.
!terraform.tfvars
